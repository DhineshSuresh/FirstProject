using System;
using iText.Layout;

namespace PDF.Controllers
{
    internal class XMLWorker
    {
        private Document document;

        public XMLWorker(Document document)
        {
            this.document = document;
        }

        internal void ToString(string v)
        {
            throw new NotImplementedException();
        }
    }
}